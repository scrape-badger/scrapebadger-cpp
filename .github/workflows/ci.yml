name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y libcpprest-dev libboost-all-dev cmake
      - run: cmake -B build && cmake --build build -j
